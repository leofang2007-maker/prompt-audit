# Static SPA serving inside the web container. The EDGE nginx (root compose) routes /api → server;
# this one just serves the built SPA with history-fallback.
server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  location / {
    try_files $uri $uri/ /index.html;
  }
}
